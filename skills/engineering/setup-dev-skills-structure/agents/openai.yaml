interface:
  display_name: "Setup Dev Skills Structure"
  short_description: "Configure a project's dev-skills structure"
  default_prompt: "Use $setup-dev-skills-structure to configure this project's tracker, triage labels, and domain docs."
policy:
  allow_implicit_invocation: false
